# Subscription chart continuation, 6 September 2026

- Resume the interrupted own-usage analysis and deliver an updated image and graph.
- Preserve the original subscription-cost comparison from the task “Estimate $200 token costs” (4 September): included subscriber usage, actual plan fees, Artificial Analysis coding-agent scores and API task costs; do not substitute API prices as subscription prices.
- Preserve all 15 previously published configurations, including Gemini through its native Antigravity harness. Existing screenshot rows remain the dated source when absent from today's dataset.
- Recover the 5 September local changes before editing. Distinguish local usage observations, extrapolated scenarios, community proxies and contractual quotas.
- Do not call mixed-model, multi-account, overage-inclusive Claude usage a measured Fable 5.1 subscription allowance.
- GLM 5.3 has no matched Coding Agent Index row on 6 September. Older GLM 5.1/5.2 rows cannot stand in for it; current Coding Plan routes both aliases to 5.3.
- Replace the draft GLM saturation estimate with the published weekly-credit bound; include recorded GLM usage in a separate graph without inventing a score.
- Deliver a reproducible main chart, a usage graph and a readable phone asset. Generate plots from reviewed JSON, inspect exports, keep sources and computation details in methodology.
- Synchronize the article, downloadable data, image labels and alt text. Verify the actual live artifacts before claiming publication.

Chart contract: compare benchmark performance against estimated subscription dollars per benchmark attempt in a scatter; exact costs in a ranked dot-range plot. Separately show three local usage totals on a common 30-day pace, with explicit periods, model mix and provisional provenance. Main plot retains group markers and neutral text; local usage uses one blue root. Export PNG/SVG with no decorative panels or tiny methodological text. Usage is evidence about workload consumption, not guaranteed subscription capacity.

User clarification: use the agent benchmark and make GLM visible. Added historical GLM 5.1/5.2 Coding Agent Index results as open squares, with clearly labeled current Pro quota transfer scenarios; do not relabel them GLM 5.3. User reports hitting the GLM plan caps.

Latest correction: replace stretched phone poster with responsive native charts. Original AA API task costs and subscription scenarios must be side by side on desktop, stacked at narrow widths, with identical score and cost scales. Include current actual Cursor Pro $20 plan and its matched Composer2.5Fast scenario; retain other Cursor agent rows with missing subscription prices explicit. Recheck GLM5.3 against agent records, distinguishing generic model catalog mentions from agent scores. Show GLM5.3 usage as usage, and supply a downloadable side-by-side image. QA must inspect text scale, overlaps and actual visual output, not merely horizontal overflow. The prior common-30-day wording is superseded by separate explicitly dated recorded periods.

Latest zoom request: focus the frontier segment (55–72 points), label every dot on both panels and all exports, and ignore Composer and historical GLM in the view. Keep both axes matched and preserve source data. Older Cursor configurations below 55 are outside this view. Avoid overlapping labels using short leader lines.

Latest visual revision (2026-09-06): linear subscription range roughly five cents to one dollar; use $0.03–$1 to retain Gemini 3.7. API uses linear $1–$12 and the same 55–72 score axis. This supersedes matched logarithmic cost axes. Remove Luna max from the visible frontier; retain all source rows. Label every point with harness and model, such as Claude Code / Fable 5.1 max and Codex / Astra med. Color by lab: OpenAI black, Anthropic orange (Opus and Fable together), Gemini rainbow, Grok slate, Kimi blue. Add soft amorphous regions only around Astra medium through max, all Anthropic configurations and both Geminis. Regions denote families, not uncertainty. Keep estimate ranges and indicate any clipped upper range with an arrow. Export desktop and phone images and verify the live page.

Follow-up visual revision (2026-09-06): move Kimi outside the subscription view with a labeled arrow showing its actual cost; tighten that linear axis to $0.03–$0.35 so the remaining central estimates fill the width. Preserve the API panel scale and its Kimi dot. Expand the Codex region to Astra medium/high/xhigh/max plus Sol max. Grok is green. Remove all persistent selected-value and explanatory text below the legend, and remove footer text from image exports. Keep complete values accessible through point tooltips and the full dataset.
